#!/bin/sh
set -eu
if ! command -v "${AILANG_BIN:-ailang}" >/dev/null 2>&1; then
  echo 'Err#err1(code=TARGET001 message="ailang executable not found." nodeId=target-macos)' >&2
  exit 2
fi
echo 'target-macos doctor: ok'
exit 0
